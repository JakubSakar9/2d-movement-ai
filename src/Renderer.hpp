//This class is created for future use (multithreading)
#include <SDL2/SDL.h>
#include "Params.hpp"

class Renderer {
    public:
        //SDL_Renderer *renderer;
        //void RenderEntities(SDL_Renderer *renderer);
};